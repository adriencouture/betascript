<li class="scene", id="<%= scene.id %>">
  <div class="scene-heading">
    <%= scene.heading %>
  </div>
  <div class="scene-action">
    <%= scene.action %>
  </div>

    <% scene.characters.each  do |char| %>

      <% if char.name.present? %>
        <div class="character-name">
          <%= char.name %>
        </div>
      <% end %>

      <% if char.parenthetical.present? %>
        <div class="character-parenthetical">
          (<%= char.parenthetical %>)
        </div>
      <% end %>

      <% if char.dialogue.present? %>
        <div class="character-dialogue">
          <%= char.dialogue %>
        </div>
      <% end %>

    <% end %>

    <% if @project.user == current_user %>
       <div id="edit-scene">
         <% link_to "Scene", anchor: "#{scene.id}" %>
         <%= link_to "Edit Scene", edit_project_scene_path(@project, scene.id) %>
       </div>
     <% end %>
</li>
